name: Deploy to GitHub Pages

on:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 20
          cache: npm
          cache-dependency-path: unimetric/package-lock.json

      - name: Install dependencies
        run: npm ci
        working-directory: unimetric

      - name: Build and deploy
        run: npm run deploy
        working-directory: unimetric
        env:
          GH_TOKEN: ${{ secrets.GITHUB_TOKEN }}
